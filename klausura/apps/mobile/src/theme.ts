import { LIGHT, SPACE, TYPE, HIT_TARGET_MIN, RADIUS } from '@klausura/ui-tokens';

/**
 * Dieselben Tokens wie auf dem Desktop, nur als React-Native-Werte. Die
 * Quelle bleibt @klausura/ui-tokens — hier wird nichts abgeschrieben.
 */
export const C = LIGHT;
export const S = SPACE;
export const T = TYPE;
export const HIT = HIT_TARGET_MIN;
export const R = RADIUS;

export const MONO = 'Menlo';
