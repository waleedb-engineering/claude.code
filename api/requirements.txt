# ClipForge API / Pipeline dependencies
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
anthropic>=0.40.0
faster-whisper>=1.0.0
pydantic>=2.0.0
# Smart-Reframe: lokale Gesichtserkennung (OpenCV Haar-Cascade), headless ohne
# GUI-/System-GL-Abhängigkeiten. Optional: fehlt es, fällt Reframe auf Center.
opencv-python-headless>=4.8.0
# YouTube-OAuth-Token-Ablage (Phase 2, Option B): OS-Keychain via keyring.
# OPTIONAL & defensiv importiert — fehlt keyring oder ein nutzbares Backend,
# meldet die Readiness sauber "blocked" (kein Plaintext-Fallback, kein Crash).
keyring>=24.0.0
# YouTube-OAuth Token-Exchange (echter Authorization-Code→Token-Tausch inkl.
# PKCE) über die offizielle Google-Library. OPTIONAL & defensiv importiert:
# fehlt sie, meldet der Callback sauber "exchange_dependency_missing" (kein
# Crash, kein Token).
google-auth>=2.0.0
google-auth-oauthlib>=1.0.0
# YouTube Private Upload (videos.insert, resumable) über die offizielle Library.
# OPTIONAL & defensiv importiert: fehlt sie, meldet der Upload sauber
# "upload_dependency_missing" (kein Crash, kein Upload). Nur für den echten
# Private-Upload nötig; Tests laufen ohne diese Library (Google wird gemockt).
google-api-python-client>=2.0.0
